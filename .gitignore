node_modules/
dist/
build/
.env
.env.local
*.log
*.tsbuildinfo
.DS_Store

# Runtime-uploaded content (course covers, QR codes, payment proofs, notes, etc.)
# — never commit real user uploads. These directories are created automatically
# on first upload, so nothing needs to be preserved here.
server/uploads/
server/secure-uploads/
